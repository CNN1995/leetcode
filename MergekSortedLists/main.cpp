#include <iostream>
#include "ListNode"
using namespace std;

class Solution {
public:
    ListNode* mergeKLists(vector<ListNode*>& lists) {
        int len = lists.size();

    }
    //¶þÂ·¹é²¢
    void merge(ListNode* &head1, ListNode* &head2){
        ListNode* p = (ListNode*)molloc(sizeof(ListNode));

    }
};
int main()
{
    cout << "Hello world2!" << endl;
    return 0;
}
